---
- hosts: localhost
  connection: local
  ignore_errors: yes
  
  tasks:
  - name: install packages
    package: 
      name: 
        - iTerm2
        - spectacle

  - name: install powerline-fonts
    script: powerline-fonts.sh
